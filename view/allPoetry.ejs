<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todas as poesias</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
    rel="stylesheet"integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" 
    crossorigin="anonymous">
</head>

<style>
    .bg-purple{background-color:#6d5c81};
</style>

<body class="bg-dark">
    <div class="container w-75 p-5 mt-4">
        <a href="/admin/addPoetry" class="text-muted"><h4>Add Poetry</h4></a>
        
        <%for(let i= 0; i < poetry.length; i++){%>
            <div id="<%=poetry[i]._id%>" class="mt-5 m-auto">
                <form method="POST" action="/admin/allPoetry/<%=poetry[i]._id%>?_method=DELETE" class="mt-2 d-flex align-items-center justify-content-between " >

                    <div class="d-flex flex-column p-2" style="white-space: pre-wrap">
                        <h2 class="text-white mb-3"><%=poetry[i].title%></h2>
                        <div class="text-white mb-3" ><%=poetry[i].text%></div>
                        <div class="text-light"><%=poetry[i].author%></div>
                        <div class="text-muted"><%=poetry[i].url%></div>
                        <div class="text-muted"><%=poetry[i].tags%></div>
                        <div class="text-muted">Click:<%=poetry[i].click%></div>
                    </div>
                    <div class="d-flex flex-column p-1">
                        <input class="bg-purple text-light mb-2 invisible "name="id" value="<%=poetry[i]._id%>">
                        <button class="btn bg-purple text-light mb-2" type="submit" >Delete</button>
                        <a href="/admin/editPoetry/<%=poetry[i]._id%>" class="btn mb-1 bg-purple text-light">Edit</a>
                    </div>
                </form>
            </div>
        <%}%>
    </div>
</body>
</html>